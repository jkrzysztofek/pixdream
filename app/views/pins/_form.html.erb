
<%= form_for @pin, html: { multipart: true } do |f| %>
  <% if @pin.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pin.errors.count, "error") %> prohibited this pin from being saved:</h2>

      <ul>
      <% @pin.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  
  <div class="form-group">
    <%= f.label :image, "Obrazek" %>
    <%= f.file_field :image, class: "form-control", required: true %>
  </div>
  <div class="form-group">
    <%= f.label :description, "Opis" %><br>
    <%= f.text_field :description, class: "form-control" %>
  </div>
  <div class="form-group">
    <%= f.label :board, "Wybierz tablicę" %>
    <option selected><%= f.collection_select(:board_id, current_user.boards, :id, :name, { include_blank: true }, {class: "form-control"}) %></option>
  </div>
  <div class="actions">
    <%= f.submit "Dodaj obrazek", class: "btn btn-primary" %>
  </div>

<% end %>
